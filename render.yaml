services:
  - type: web
    name: ppe-style-engine
    runtime: docker
    plan: starter
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
    disk:
      name: ppe-data
      mountPath: /app/db
      sizeGB: 1
